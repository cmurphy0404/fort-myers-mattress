---
const valueProps = [
  {
    icon: 'users',
    title: 'No Employees',
    description: "Operating with zero employees means we don't have to charge premium prices for our mattresses to pay other people.",
  },
  {
    icon: 'tv',
    title: 'Inexpensive Advertising',
    description: "We don't spend thousands and thousands of dollars every month on TV, radio, and billboards like our competition.",
  },
  {
    icon: 'calendar',
    title: 'Appointment Based',
    description: "Our store hours are by appointment only. This allows us to keep our utility bills 80% lower than the competition! Plus as an added bonus, you get the entire place to yourself when you come in!",
  },
];
---

<section class="section-padding bg-white">
  <div class="container-wide">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        How Are We Able To Save You Money?
      </h2>
    </div>

    <div class="grid sm:grid-cols-2 md:grid-cols-3 gap-4 sm:gap-6 md:gap-8 mb-16">
      {valueProps.map((prop) => (
        <div class="text-center p-5 sm:p-6 rounded-2xl bg-gray-50 hover:bg-blue-50 transition-colors duration-300">
          <div class="w-14 h-14 sm:w-16 sm:h-16 mx-auto mb-4 sm:mb-6 bg-accent/10 rounded-full flex items-center justify-center">
            {prop.icon === 'users' && (
              <svg class="w-7 h-7 sm:w-8 sm:h-8 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
            )}
            {prop.icon === 'tv' && (
              <svg class="w-7 h-7 sm:w-8 sm:h-8 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
            )}
            {prop.icon === 'calendar' && (
              <svg class="w-7 h-7 sm:w-8 sm:h-8 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
            )}
          </div>
          <h3 class="text-lg sm:text-xl font-semibold text-gray-900 mb-2 sm:mb-3">{prop.title}</h3>
          <p class="text-sm sm:text-base text-gray-600">{prop.description}</p>
        </div>
      ))}
    </div>

    <!-- What To Expect Section -->
    <div class="max-w-5xl mx-auto">
      <div class="text-center mb-8 sm:mb-10">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-2">
          What To Expect
        </h2>
        <p class="text-gray-600">Real savings, real convenience</p>
      </div>

      <div class="grid sm:grid-cols-3 gap-4 sm:gap-6">
        <!-- Savings Card -->
        <div class="relative overflow-hidden bg-gradient-to-br from-green-50 to-emerald-50 border border-green-200 rounded-2xl p-6 sm:p-8 text-center group hover:shadow-lg transition-shadow duration-300">
          <div class="w-12 h-12 sm:w-14 sm:h-14 mx-auto mb-4 bg-green-100 rounded-full flex items-center justify-center">
            <svg class="w-6 h-6 sm:w-7 sm:h-7 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <div class="text-3xl sm:text-4xl font-bold text-green-700 mb-2">50-80%</div>
          <div class="text-sm sm:text-base text-green-800 font-medium">Off Retail Prices</div>
        </div>

        <!-- Payment Plans Card -->
        <div class="relative overflow-hidden bg-gradient-to-br from-blue-50 to-indigo-50 border border-blue-200 rounded-2xl p-6 sm:p-8 text-center group hover:shadow-lg transition-shadow duration-300">
          <div class="w-12 h-12 sm:w-14 sm:h-14 mx-auto mb-4 bg-blue-100 rounded-full flex items-center justify-center">
            <svg class="w-6 h-6 sm:w-7 sm:h-7 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"/>
            </svg>
          </div>
          <div class="text-3xl sm:text-4xl font-bold text-blue-700 mb-2">$20</div>
          <div class="text-sm sm:text-base text-blue-800 font-medium">Down Payment Plans</div>
          <div class="text-xs text-blue-600 mt-1">No credit needed</div>
        </div>

        <!-- Delivery Card -->
        <div class="relative overflow-hidden bg-gradient-to-br from-amber-50 to-orange-50 border border-amber-200 rounded-2xl p-6 sm:p-8 text-center group hover:shadow-lg transition-shadow duration-300 sm:col-span-1 col-span-full">
          <div class="w-12 h-12 sm:w-14 sm:h-14 mx-auto mb-4 bg-amber-100 rounded-full flex items-center justify-center">
            <svg class="w-6 h-6 sm:w-7 sm:h-7 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16V6a1 1 0 00-1-1H4a1 1 0 00-1 1v10a1 1 0 001 1h1m8-1a1 1 0 01-1 1H9m4-1V8a1 1 0 011-1h2.586a1 1 0 01.707.293l3.414 3.414a1 1 0 01.293.707V16a1 1 0 01-1 1h-1m-6-1a1 1 0 001 1h1M5 17a2 2 0 104 0m-4 0a2 2 0 114 0m6 0a2 2 0 104 0m-4 0a2 2 0 114 0"/>
            </svg>
          </div>
          <div class="text-2xl sm:text-3xl font-bold text-amber-700 mb-2">Same Day</div>
          <div class="text-sm sm:text-base text-amber-800 font-medium">Delivery Available</div>
        </div>
      </div>
    </div>
  </div>
</section>
