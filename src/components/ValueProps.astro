---
const valueProps = [
  {
    icon: 'users',
    title: 'No Employees',
    description: "Operating with zero employees means we don't have to charge premium prices for our mattresses to pay other people.",
  },
  {
    icon: 'tv',
    title: 'Inexpensive Advertising',
    description: "We don't spend thousands and thousands of dollars every month on TV, radio, and billboards like our competition.",
  },
  {
    icon: 'calendar',
    title: 'Appointment Based',
    description: "Our store hours are by appointment only. This allows us to keep our utility bills 80% lower than the competition! Plus as an added bonus, you get the entire place to yourself when you come in!",
  },
];
---

<section class="section-padding bg-white">
  <div class="container-wide">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        How Are We Able To Save You Money?
      </h2>
    </div>

    <div class="grid sm:grid-cols-2 md:grid-cols-3 gap-4 sm:gap-6 md:gap-8 mb-16">
      {valueProps.map((prop) => (
        <div class="text-center p-5 sm:p-6 rounded-2xl bg-gray-50 hover:bg-blue-50 transition-colors duration-300">
          <div class="w-14 h-14 sm:w-16 sm:h-16 mx-auto mb-4 sm:mb-6 bg-accent/10 rounded-full flex items-center justify-center">
            {prop.icon === 'users' && (
              <svg class="w-7 h-7 sm:w-8 sm:h-8 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
            )}
            {prop.icon === 'tv' && (
              <svg class="w-7 h-7 sm:w-8 sm:h-8 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
            )}
            {prop.icon === 'calendar' && (
              <svg class="w-7 h-7 sm:w-8 sm:h-8 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
            )}
          </div>
          <h3 class="text-lg sm:text-xl font-semibold text-gray-900 mb-2 sm:mb-3">{prop.title}</h3>
          <p class="text-sm sm:text-base text-gray-600">{prop.description}</p>
        </div>
      ))}
    </div>

    <!-- What To Expect Section -->
    <div class="text-center max-w-4xl mx-auto">
      <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-8 font-display">
        What To Expect...
      </h2>
      
      <div class="flex flex-col gap-4">
        <div class="bg-accent text-white py-4 px-6 rounded-lg shadow-md transform hover:scale-[1.02] transition-transform duration-300">
          <div class="text-xl sm:text-2xl md:text-3xl font-medium">
            50-80% Off Retail Store Prices
          </div>
        </div>
        <div class="bg-accent text-white py-4 px-6 rounded-lg shadow-md transform hover:scale-[1.02] transition-transform duration-300">
          <div class="text-xl sm:text-2xl md:text-3xl font-medium">
            No Credit Needed Payment Plans - Only $40 down
          </div>
        </div>
        <div class="bg-accent text-white py-4 px-6 rounded-lg shadow-md transform hover:scale-[1.02] transition-transform duration-300">
          <div class="text-xl sm:text-2xl md:text-3xl font-medium">
            Same Day Delivery!
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
