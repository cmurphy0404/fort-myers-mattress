---
const products = [
  'Memory Foam',
  'Hybrid',
  'Pillow-top',
  'Firm & Plush Options',
  'Adjustable Bases',
];
---

<section class="section-padding bg-white">
  <div class="container-wide">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Content -->
      <div>
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
          What We Carry
        </h2>
        <p class="text-lg text-gray-600 mb-8">
          We stock a curated selection of quality mattresses to match every sleep preference and budget.
        </p>
        <ul class="space-y-4">
          {products.map((product) => (
            <li class="flex items-center gap-3">
              <div class="w-6 h-6 bg-green-100 rounded-full flex items-center justify-center flex-shrink-0">
                <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
              </div>
              <span class="text-lg text-gray-700">{product}</span>
            </li>
          ))}
        </ul>
        <p class="mt-8 text-gray-500 text-sm">
          Not sure what you need? We'll help you find the right fit during your appointment.
        </p>
      </div>

      <!-- Image -->
      <div class="relative">
        <div class="rounded-2xl overflow-hidden shadow-xl">
          <img
            src="/images/mattress-display.jpg"
            alt="Selection of quality mattresses"
            class="w-full h-auto object-cover aspect-[4/3]"
            loading="lazy"
            width="600"
            height="450"
          />
        </div>
        <div class="absolute -bottom-4 -right-4 bg-accent text-white rounded-xl shadow-lg p-4 hidden md:block">
          <p class="font-semibold">Quality Selection</p>
          <p class="text-sm text-blue-100">For Every Budget</p>
        </div>
      </div>
    </div>
  </div>
</section>
